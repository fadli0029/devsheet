# Theme-level configuration. Hugo reads [module.hugoVersion] from a theme's own
# config file, not from theme.toml, so the version constraint has to live here to
# actually warn anyone. Keep this file minimal: everything in it merges into the
# consuming site's configuration.
[module]
  [module.hugoVersion]
    min = "0.122.0"
